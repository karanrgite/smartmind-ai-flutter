import 'package:flutter/material.dart';

class AppColors {
  // Shared accent across themes
  static const accent = Color(0xFFFF6A2B);

  // Dark theme
  static const darkBg = Color(0xFF0B0B0D);
  static const darkCard = Color(0xFF1A1A1D);
  static const darkInputFill = Color(0xFF1F1F22);
  static const darkHint = Color(0xFF8A8A8E);

  // Light theme
  static const lightBg = Color(0xFFF7F7F8);
  static const lightCard = Color(0xFFFFFFFF);
  static const lightInputFill = Color(0xFFEFEFF1);
  static const lightHint = Color(0xFF6E6E73);
}